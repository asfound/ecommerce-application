name: 'Setup Node Modules'
description: 'Setup Node.js, restore npm cache and install dependencies'
runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: latest

    - name: Install dependencies
      shell: bash
      run: npm install
